interface DividerProps {
  // 클래스 이름
  classNames?: string;
  // 디바이더가 시작되는 지점
  orientation?: 'left' | 'right' | 'center';
  // 디바이더에 삽입할 텍스트 문구
  plain?: string;
  // 디바이더 방향
  direction?: 'horizontal' | 'vertical';
}
