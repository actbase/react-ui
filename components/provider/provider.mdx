import { Meta, Controls } from '@storybook/blocks';
import * as ProviderStories from './provider.stories';

<Meta of={ProviderStories} />

# Provider

## API

<Controls of={ProviderStories.Provider} />
